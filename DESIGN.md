# ArguMesh 设计规范

**仓库：[github.com/Fyuan0206/ArguMesh](https://github.com/Fyuan0206/ArguMesh)** · MIT License

产品视觉方向、设计令牌与设计 QA 记录。品牌识别（logo 语义、安全空间、资产清单）见 [`docs/brand-guidelines.md`](docs/brand-guidelines.md)，本文不重复。

## 视觉方向

- **主视觉目标**：深色导航的证据矩阵概念——**论文为列 × 研究维度为行**，下方是证据核验分区。
- 用户明确偏好**简单、清晰**的界面：减少次级控件和状态噪音，保持矩阵 + 核验流程醒目。
- 配色：石墨导航、冷白/灰表面、青色操作强调、**琥珀色 = AI 草稿**、绿色 = 已确认。

## 设计令牌

定义在 `src/styles.css` 的 `:root`。新颜色必须走 token，不要写死 hex。

| Token | 值 | 用途 |
| --- | --- | --- |
| `--nav` | `#101d27` | 石墨色导航（Ink Navy） |
| `--nav-muted` | `#9eacb7` | 导航次级文字 |
| `--ink` | `#172432` | 标题与正文主色（Paper Ink） |
| `--accent` | `#0788a2` | 青色操作强调（Research Teal 系） |
| `--accent-dark` | `#056d82` | 深一级强调（Deep Teal） |
| `--line` | `#dbe2e7` | 分隔线 |
| `--muted` | `#687683` | 次级文字 |
| `--soft` | `#f5f7f8` | 浅色面板底 |
| `--draft` | `#b7791f` | **琥珀 = AI 草稿 / 待确认** |
| `--success` | `#2f8b68` | 绿色 = 已确认 |

字体：`--font-sans`（Inter / PingFang SC / Microsoft YaHei / Noto Sans SC）、`--font-serif`（Georgia / Noto Serif SC / Songti SC / SimSun）、`--font-mono`（JetBrains Mono / SF Mono / Menlo / Consolas）。

品牌色另有 Evidence Cyan `#7CE2F3`（核心节点与高亮连线），主要用于 logo 与宣传物料，不用于界面 chrome。

**AI 与人工必须可区分**：AI 生成 / 待确认用 `--draft`，用户已确认用 `--success`。这是产品规则，不只是配色偏好。

## 布局约定

| 容器类 | 用于 |
| --- | --- |
| `matrix-workspace` | `/matrices/` 详情页，全屏无侧栏 |
| `reader-workspace` | `…/library/:paperId/read` 阅读器 |
| `route-workspace` | 其他所有路由页 |

侧栏在窗口宽 ≤ 900px 时自动收起。图标统一用 `@phosphor-icons/react`。

## 产品设计 QA

**视口：1047 × 698，deviceScaleFactor 1。**

来源视觉真值：`exec-2cdd5507-87af-468f-b331-e960213759e4.png`（本机 `.codex/generated_images/` 下）。实现对照：`http://localhost:5173/projects/demo-occluded-pose`。截图产物在 `design-qa-artifacts/`（gitignored）。

### 已修

- **[P1]** 原生会话选择/按钮簇换成紧凑面包屑 + 项目状态 + 新建会话 + 可用的历史弹出层。62/38 工作区分割在标注视口下保持稳定。
- **[P1]** 实验编辑器不再是散落全页的无样式控件，改为有界、可滚动安全的 AI 设计弹窗，带研究问题接地和清晰的生成范围。
- **[P2]** 自动化弹窗截图暴露了浏览器默认的取消/关闭按钮，随后补了共享的 `.outline` 与 `.icon-button` 样式。
- **[P2]** 填充态表格截图暴露了浏览器默认的「重新分析」按钮；`.table-action` 现为紧凑、可访问的次级操作样式。
- **[P2]** 实验/结果深链接改用立即居中定位——平滑滚动会让快速导航和截图停在实验头部而不是被引用的结果行。

### 必须保持的保真面

- **字体排版**：1047 × 698 下目视检查，标注区域内无裁切、无浏览器默认排版残留。
- **间距与布局节奏**：Agent 头部、mission rail、实验流程、弹窗均已在该视口截图为界内。
- **颜色与视觉令牌**：ink/teal 研究工作台色板一致；**紫色仅用于 AI 出处与生成提示**。
- **图像与资产保真**：Phosphor 图标在 dsf 1 下渲染干净；这些表面不用位图 UI 资产。
- **文案与内容**：标题、辅助文字、控件在三张截图里均无碰撞。
- **响应式与无障碍状态**：键盘弹窗处理与响应式 CSS 已实现并被构建/测试覆盖；只有标注的桌面视口有像素级证据。

### 未覆盖 / 待确认

- 自动化截图用的是 headless Edge，**不是用户实际选用的内置浏览器实例**。用户浏览器的刷新截图仍是最终同浏览器确认的前置条件。
- 未截：填充态会话消息、历史弹出层、用户确切浏览器实例。
- 本机 Windows Computer Use 运行时不可用，无法在用户浏览器里做交互回放与 console 检查；用固定视口 headless Edge 替代产出证据。

### 迭代历史

0. 来源视觉确定，实现路由打开；因缺同状态实现截图，首次有效比对被阻塞。
1. 用户提供 1047 × 698 浏览器证据，显示原生 select/按钮构成的会话头部过于拥挤 → 重建为项目面包屑、项目状态、扁平的新建会话动作、可用的历史弹出层。
2. 用户提供证据，显示实验创建表单缺弹窗布局、原生控件散落全页 → 重建为 AI 优先的三步工作台（结构化设计、消融、导入结果、AI 分析表格），补弹窗与响应式样式。
3. 自动化修后截图确认重新设计的 Agent 头部、三步实验工作台、弹窗布局；弹窗截图暴露浏览器默认次级/图标按钮 → 补共享样式，二次截图确认。
4. 完整填充的实验夹具确认主设计卡片、两行消融表、分析汇总表、证据 chip、支持状态、原始数据披露；由此引入确定性的结果深链接滚动与 refined 表格操作。

### 收尾清单

1. 拿到一张用户选用浏览器里的修后刷新截图。
2. 对照自动化 1047 × 698 证据，比对浏览器专属字体度量与换行。
3. 若用户浏览器截图仍暴露 P0/P1/P2 问题，修完重截。

后续抛光：等项目里有了真实内容，可对填充态会话与结果表格做一轮 P3。

**当前结论**：自动化视口 QA 通过；同浏览器最终确认待定。

## 官网设计

公开站点（`website/`，`https://argumesh.nekocfy.com/`）的设计 QA 单独记录在部署文档里——见 [`docs/DEPLOYMENT.md`](docs/DEPLOYMENT.md) 的「官网」一节。要点：

- 参考对象 `agentero.app/zh`，做的是**格式适配而非照搬品牌**。
- 深色页面、胶囊导航、居中衬线品牌名、灰色中文主标题、成对 CTA、大产品截图建立要求的层级。
- 首屏后来换成 PDF 阅读器截图——最初的 Research Agent 截图里有「AI 未配置」报错。
- 桌面与安装分区做过目视检查；390px 移动视口无横向溢出。
- 矩阵与研究脉络截图展示的是初始进入态；更充分的演示截图能更好传达能力。
